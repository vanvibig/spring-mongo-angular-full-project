server:
  port: 8082
spring:
  application:
    name: springboot-mongo-angular
  data:
    mongodb:
      host: localhost
      port: 27017
      database: spring_mongo
      grid-fs-database: images

logging:
  level:
    com.example: DEBUG
    org.springframework.web: DEBUG